# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Ambiente / segredos
.env
.env.*
!.env.example
*.key
*.pem
service-account*.json
credentials*.json

# Dados
*.csv
*.parquet
*.duckdb
data/
!data/.gitkeep

# Ferramentas
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# IDE / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
.tools/

# Terraform — o cache do provider e o estado ficam fora; o `.terraform.lock.hcl`
# é versionado de propósito: ele é a trava de versão do provider, e o projeto
# fixa imagem por digest e dependência por lock (ADR-0026).
.terraform/
terraform.tfstate*
dbt/.user.yml

# Estado de execução do produtor de streaming (Etapa 7) — não é código.
.stream/
